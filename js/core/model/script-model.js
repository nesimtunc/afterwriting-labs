define(function(require) {

    var Protoplast = require('protoplast');

    var ScriptModel = Protoplast.Model.extend({
        
        script: 'Init...'
        
    });

    return ScriptModel;
});